{% extends 'base.html.twig' %}

{% block title %}Menu Cart | Dish Detail{% endblock %}

{% block body %}

    <br />

    <div class="container">
        <h1>{{ dish.name }}</h1>

        <img src="{{ asset('images/' ~ dish.image) }}">
    </div>
{% endblock %}
